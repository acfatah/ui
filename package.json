{
  "name": "@acfatah/ui",
  "homepage": "https://github.com/acfatah/ui",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "cd packages/registry && bun run lint",
    "format": "cd packages/registry && bun run format",
    "typecheck": "cd packages/registry && bun run typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "~6.0.3"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide",
    "esbuild"
  ]
}
