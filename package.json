{
  "name": "@acfatah/ui",
  "homepage": "https://github.com/acfatah/ui",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "bun run --filter './packages/*' lint",
    "format": "bun run --filter './packages/*' format",
    "typecheck": "bun run --filter './packages/*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "~6.0.3"
  },
  "trustedDependencies": [
    "@tailwindcss/oxide",
    "esbuild"
  ]
}
